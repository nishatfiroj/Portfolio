import Image from "next/image"
import styled from "styled-components"

export const VinylContainer = styled(Image)`
  margin: 8px;
`
export const Caption = styled.p`
  font-size: 12px;
  margin: 12px 64px;
`
